<Page x:Class="ToxicBizBuddyWPF.Views.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:components="clr-namespace:ToxicBizBuddyWPF.Components"
      Title="ClientsPage">

    <components:PageWrapper>
        <StackPanel>

            <TextBlock Text="Clientes" Style="{StaticResource H1}"/>
            <TextBlock Text="Lista de clientes registrados"
                 Style="{StaticResource Subtle}" Margin="0,4,0,16"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Button Style="{StaticResource BtnPrimary}" Content="+ Agregar cliente"/>
                <Button Style="{StaticResource BtnGhost}" Content="Importar" Margin="12,0,0,0"/>
                <ContentControl Style="{StaticResource Badge}" Margin="12,0,0,0" Content="Total: 0"/>
            </StackPanel>

            <Border Style="{StaticResource CardStyle}">
                <DataGrid AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  Padding="4"
                  Style="{StaticResource DataGridBase}"
                  ColumnHeaderStyle="{StaticResource DataGridHeader}"
                  RowStyle="{StaticResource DataGridRowStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre"   Binding="{Binding Nombre}"   Width="*"/>
                        <DataGridTextColumn Header="DNI"      Binding="{Binding DNI}"      Width="150"/>
                        <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="150"/>
                        <DataGridTextColumn Header="Email"    Binding="{Binding Email}"    Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </StackPanel>
    </components:PageWrapper>
</Page>

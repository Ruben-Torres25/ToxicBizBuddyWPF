<Page x:Class="ToxicBizBuddyWPF.Views.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Orders">

    <!-- ===== Recursos locales ===== -->
    <Page.Resources>

        <!-- Colores -->
        <SolidColorBrush x:Key="Slate100" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="Slate200" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextStrong" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSubtle" Color="#6B7280"/>

        <!-- Botón primario -->
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1F56D6"/>
                                <Setter Property="BorderBrush" Value="#1F56D6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#194AB9"/>
                                <Setter Property="BorderBrush" Value="#194AB9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
        </Style>

        <!-- Search input -->
        <Style x:Key="SearchBoxBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <!-- Botón fantasma -->
        <Style x:Key="BtnGhost" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                <Setter Property="Foreground" Value="#2563EB"/>
                                <Setter Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón icono Acciones -->
        <Style x:Key="IconPill" TargetType="Button" BasedOn="{StaticResource BtnGhost}">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                <Setter Property="Foreground" Value="#2563EB"/>
                                <Setter Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Badges -->
        <Style x:Key="BadgeBase" TargetType="Border">
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="BadgeSuccess" TargetType="Border" BasedOn="{StaticResource BadgeBase}">
            <Setter Property="Background" Value="#E8FFF3"/>
            <Setter Property="BorderBrush" Value="#BBF7D0"/>
        </Style>
        <Style x:Key="BadgeWarn" TargetType="Border" BasedOn="{StaticResource BadgeBase}">
            <Setter Property="Background" Value="#FFF7ED"/>
            <Setter Property="BorderBrush" Value="#FED7AA"/>
        </Style>
        <Style x:Key="BadgeDanger" TargetType="Border" BasedOn="{StaticResource BadgeBase}">
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="BorderBrush" Value="#FCA5A5"/>
        </Style>

        <!-- DataGrid limpio -->
        <Style x:Key="OrdersGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFF"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Slate200}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Slate200}"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSubtle}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="4"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="{StaticResource Slate200}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ====== TEMPLATES para Estado (evitan recursividad) ====== -->
        <DataTemplate x:Key="TplCompleted">
            <Border Style="{StaticResource BadgeSuccess}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE73E;" Foreground="#16A34A" Margin="0,0,6,0"/>
                    <TextBlock Text="Completado" Foreground="#16A34A"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="TplPending">
            <Border Style="{StaticResource BadgeWarn}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE823;" Foreground="#D97706" Margin="0,0,6,0"/>
                    <TextBlock Text="Pendiente" Foreground="#D97706"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="TplCanceled">
            <Border Style="{StaticResource BadgeDanger}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE711;" Foreground="#DC2626" Margin="0,0,6,0"/>
                    <TextBlock Text="Cancelado" Foreground="#DC2626"/>
                </StackPanel>
            </Border>
        </DataTemplate>

    </Page.Resources>

    <!-- ===== Contenido ===== -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#FAFAFF">
        <StackPanel Margin="24">

            <!-- Header -->
            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Gestión de Pedidos" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextStrong}"/>
                    <TextBlock Text="Administra todos los pedidos de tus clientes" Margin="0,6,0,0" Foreground="{StaticResource TextSubtle}"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource BtnPrimary}" Click="NewOrder_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" Margin="0,0,8,0"/>
                        <TextBlock Text="Nuevo Pedido"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Search + filtros -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource SearchBoxBorder}" Margin="0,0,8,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   FontFamily="Segoe MDL2 Assets"
                                   Text="&#xE721;"
                                   Foreground="{StaticResource TextSubtle}"
                                   VerticalAlignment="Center" Margin="2,0,8,0"/>
                        <TextBox Grid.Column="1"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 Foreground="{StaticResource TextStrong}"
                                 ToolTip="Buscar por cliente o número de pedido..."/>
                    </Grid>
                </Border>

                <Button Grid.Column="1" Style="{StaticResource BtnGhost}" Click="Filters_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE71C;" Margin="0,0,6,0"/>
                        <TextBlock Text="Filtros"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Lista -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Lista de Pedidos" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <DataGrid x:Name="OrdersGrid" Style="{StaticResource OrdersGrid}" ItemsSource="{Binding}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Pedido" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock TextDecorations="Underline" Foreground="#2563EB" Text="{Binding Pedido}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="2*"/>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="1.5*"/>
                            <DataGridTextColumn Header="Items" Binding="{Binding Items}" Width="100"/>

                            <DataGridTemplateColumn Header="Total" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="$"/>
                                            <Run Text="{Binding Total}" FontWeight="Bold"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Estado con ContentTemplate (sin recursión) -->
                            <DataGridTemplateColumn Header="Estado" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ContentControl>
                                            <ContentControl.Style>
                                                <Style TargetType="ContentControl">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Completado">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource TplCompleted}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Pendiente">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource TplPending}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Cancelado">
                                                            <Setter Property="ContentTemplate" Value="{StaticResource TplCanceled}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Acciones -->
                            <DataGridTemplateColumn Header="Acciones" Width="190">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                            <Button Style="{StaticResource IconPill}" ToolTip="Ver" Click="ViewOrder_Click">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8A7;"/>
                                            </Button>
                                            <Button Style="{StaticResource IconPill}" ToolTip="Editar" Margin="8,0,0,0" Click="EditOrder_Click">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;"/>
                                            </Button>
                                            <Button Style="{StaticResource IconPill}" ToolTip="Duplicar" Margin="8,0,0,0" Click="DuplicateOrder_Click">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8C8;"/>
                                            </Button>
                                            <Button Style="{StaticResource IconPill}" ToolTip="Eliminar" Margin="8,0,0,0" Click="DeleteOrder_Click">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>

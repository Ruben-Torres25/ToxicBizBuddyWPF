<Page x:Class="ToxicBizBuddyWPF.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:components="clr-namespace:ToxicBizBuddyWPF.Components"
      Title="ProductsPage">

    <components:PageWrapper>
        <StackPanel>

            <!-- Título -->
            <components:PageHeader Title="Productos" Subtitle="Listado actualizado de productos disponibles"/>

            <!-- Botón Agregar -->
            <components:PrimaryButton Text="Agregar producto"/>

            <!-- Tabla en card -->
            <components:Card>
                <DataGrid AutoGenerateColumns="False" CanUserAddRows="False"
                          HeadersVisibility="Column"
                          RowHeight="36"
                          ColumnHeaderHeight="36"
                          FontSize="14"
                          BorderThickness="0"
                          Background="White"
                          GridLinesVisibility="None"
                          AlternatingRowBackground="#F1F5F9"
                          Padding="4">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                        <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="100" />
                        <DataGridTextColumn Header="Precio" Binding="{Binding Precio}" Width="100" />
                        <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </components:Card>

        </StackPanel>
    </components:PageWrapper>
</Page>
